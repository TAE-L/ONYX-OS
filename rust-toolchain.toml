[toolchain]
# Pinned to 2026-09-08: the floating `nightly-x86_64-pc-windows-gnu` channel
# auto-updated to 1.100.0-nightly (2026-08-27), whose LLVM backend regressed
# on this kernel's inline asm (`error: offset is not a multiple of 16` on a
# clean codegen). 2026-09-08 (rustc cea272fa3) codegens it fine.
channel = "nightly-2026-09-08-x86_64-pc-windows-gnu"
components = ["llvm-tools", "rust-src"]
targets = ["x86_64-unknown-none", "x86_64-unknown-uefi"]