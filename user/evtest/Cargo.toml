[package]
name = "user-evtest"
version = "0.1.0"
edition = "2021"

# Bin name matters: the kernel's build script looks for the
# CARGO_BIN_FILE_USER_EVTEST_evtest artifact-dependency variable.
[[bin]]
name = "evtest"
path = "src/main.rs"

[dependencies]