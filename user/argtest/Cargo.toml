[package]
name = "user-argtest"
version = "0.1.0"
edition = "2021"

# Bin name matters: the kernel's build script looks for the
# CARGO_BIN_FILE_USER_ARGTEST_argtest artifact-dependency variable.
[[bin]]
name = "argtest"
path = "src/main.rs"

[dependencies]